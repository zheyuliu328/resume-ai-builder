# ============================================
# Cline 成本优化规则 - Resume AI Builder
# ============================================
# 基于社区最佳实践，针对大型仓库的成本控制

## 项目信息
- 项目类型：Python Web 应用 + 前端界面
- 核心文件：app.py, backend/api_server.py, frontend/
- 数据文件：resume_data.json, config.json

## 🎯 成本优化策略

### 模型使用策略（分层处理）
- **规划阶段**：使用 Claude 3.5 Haiku（便宜模型）进行初步分析和规划
- **代码修改**：使用 Claude 3.5 Sonnet（平衡性价比）进行精确修改
- **复杂重构**：仅在必要时使用高级模型

### 上下文控制
- 最大请求数：**3-5** （避免无限循环推理）
- 优先读取核心代码文件，避免文档和示例文件
- 使用 .clineignore 排除大量无关内容

### 工作流程优化
1. **先规划后执行**：复杂任务先生成详细计划
2. **分步骤处理**：将大任务拆分为小步骤
3. **避免重复分析**：利用已有的项目理解

## 📋 编码规范
- 简洁的 Python 代码，遵循 PEP 8
- 中文注释和文档
- 最小化修改原则：只改必要的部分

## 🚫 禁止操作
- 不删除用户数据文件（resume_data.json, config.json）
- 不删除 .env 配置文件
- 不暴露 API keys 或敏感信息
- 不进行不必要的大规模重构

## 🔄 API 稳定性保障

### 自动重试机制
遇到以下错误时自动重试：
- 502 Bad Gateway
- 500 Internal Server Error  
- 503 Service Unavailable
- 429 Too Many Requests

重试策略：
1. 第1次错误：等待 5 秒后重试
2. 第2次错误：等待 10 秒后重试
3. 第3次错误：等待 20 秒后重试
4. 持续失败：等待 60 秒循环重试

### 智能工具切换
- API 错误时自动切换备选方案
- 操作超时（60秒）自动切换策略
- 优先使用本地工具而非外部 API

## 📁 项目结构理解

### 核心文件（高优先级）
- `app.py` - 主应用入口
- `backend/api_server.py` - API 服务器
- `frontend/index.html` - 前端界面
- `frontend/app.js` - 前端逻辑
- `requirements.txt` - 依赖管理

### 配置文件（中优先级）
- `config.json` - 应用配置
- `resume_data.json` - 简历数据
- `.env` - 环境变量

### 辅助文件（低优先级）
- 文档文件（*.md）
- 测试脚本（test_*.py）
- 示例文件（*.example.*）

## 🎛️ 任务处理策略

### 简单任务（1-2 步骤）
- 直接执行，无需详细规划
- 使用便宜模型即可

### 中等任务（3-5 步骤）
- 先生成简要计划
- 分步骤执行和验证

### 复杂任务（6+ 步骤）
- **必须先进入 PLAN 模式**
- 详细分析需求和影响范围
- 生成完整的执行计划
- 用户确认后再执行

## 🔍 调试和测试

### 测试策略
- 优先使用轻量级测试
- 避免启动完整的浏览器测试（除非必要）
- 使用 `test_quick.py` 进行快速验证

### 错误处理
- 记录错误但不重复尝试相同方法
- 提供替代解决方案
- 必要时请求用户确认

## 💡 成本节省技巧

### 减少 Token 消耗
1. **精准文件读取**：只读取相关文件，避免批量读取
2. **增量修改**：使用 replace_in_file 而非重写整个文件
3. **避免重复分析**：利用上下文中的已有信息

### 智能缓存利用
- 记住项目结构和核心逻辑
- 避免重复读取相同文件
- 利用 .claude/skills/ 中的已有解决方案

### 批量操作优化
- 将多个小修改合并为一次操作
- 避免频繁的文件读写
- 使用单次命令完成多个步骤

## 📊 性能监控

### 关键指标
- 每个任务的 token 使用量
- 模型调用次数
- 任务完成时间

### 优化目标
- 单个任务控制在 **1000-2000 tokens** 以内
- 模型调用次数不超过 **5 次**
- 任务完成时间控制在 **2 分钟**以内

## 🚀 快速启动命令

### 开发服务器
```bash
cd /Users/zheyuliu/Documents/GitHub/resume-ai-builder
python app.py
```

### 快速测试
```bash
python test_quick.py
```

### 生产部署
```bash
./start_server.sh
```

---
*此配置基于社区最佳实践，旨在在保持功能完整性的同时最大化成本效益*